using System;
using System.Collections.Generic;

using Xamarin.Forms;

namespace NeMonopolia3
{
    public partial class ActionView : ContentPage
    {
        public ActionView()
        {
            InitializeComponent();
        }

      async  void Button_Clicked(System.Object sender, System.EventArgs e)
        {
            ///Buy
            //var player = Mocks.GetPlayerCharacteristic();
            //var factory = Mocks.GetFactories()[0];
          //  Manipulation.BuyFactory(player,factory);
           // await Navigation.PushAsync(new Bag(player.Portfolio));
        }
    }
}

